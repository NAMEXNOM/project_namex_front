/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Aquí puedes poner más opciones si las necesitas
};

module.exports = nextConfig; // Esta línea es la que soluciona el Warning
